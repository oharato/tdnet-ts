[project]
name = "tdnet-ts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "oharato", email = "oharato@live.jp" }
]
requires-python = ">=3.12"
dependencies = [
    "camelot-py[cv]>=1.0.9",
    "opencv-python-headless>=4.13.0.92",
]

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"
